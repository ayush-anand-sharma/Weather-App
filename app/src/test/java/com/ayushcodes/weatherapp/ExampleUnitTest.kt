package com.ayushcodes.weatherapp // Defines the package for unit tests

import org.junit.Test // Imports Test annotation
import org.junit.Assert.* // Imports assertions

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * See [testing documentation](http://d.android.com/tools/testing).
 */
// Example unit test class
class ExampleUnitTest {
    // Test method to verify addition
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2) // Assert that 2 + 2 equals 4
    }
}