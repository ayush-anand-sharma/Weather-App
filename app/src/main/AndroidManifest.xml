<?xml version="1.0" encoding="utf-8"?>
<!-- Manifest file definition with android namespace -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permission required to access the internet for API calls -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Permission required to check the network connectivity state -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Permission required to access fine location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Permission required to access coarse location -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Application configuration block -->
    <application
        android:usesCleartextTraffic="true" 
        android:allowBackup="true" 
        android:dataExtractionRules="@xml/data_extraction_rules" 
        android:fullBackupContent="@xml/backup_rules" 
        android:icon="@mipmap/ic_launcher" 
        android:label="@string/app_name" 
        android:roundIcon="@mipmap/ic_launcher_round" 
        android:supportsRtl="true" 
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"> 
        
        <!-- MainActivity configuration -->
        <activity
            android:name=".MainActivity" 
            android:exported="true" /> 
        
        <!-- SplashScreen activity configuration -->
        <activity
            android:name=".SplashScreen" 
            android:exported="true"> 
            
            <!-- Intent filter to make this the launch activity -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" /> 
                <category android:name="android.intent.category.LAUNCHER" /> 
            </intent-filter>
        </activity>
    </application>

</manifest>